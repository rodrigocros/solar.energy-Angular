import { IunidadeConsumidora } from "./IunidadeConsumidora";

class unidadeMes implements IunidadeConsumidora {
    constructor(mesAno:string, totaKw:number) {
        let data = mesAno
    }
    id: any =0;
    apelido: string = "";
    local: string = "";
    marca: string = "";
    modelo: string= "";
    ativo: boolean = false
}